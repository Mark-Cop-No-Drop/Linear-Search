def linearSearch(arr, targetVal):
    count = 0
    for i in range(len(arr)):
        if arr[i]==targetVal:
            count+=1
    return count
myList=[1,3,5,7,5,9,5]
x=5
result=linearSearch(myList,x)
TimesFound= 0
if result == 0:
    print("Letter NOT found")
else:
    TimesFound + 1
    print("Letter FOUND, times found : ", result   )
